#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define all(x) (x).begin(), (x).end()

template <typename T>
T nxt() {
  T x;
  cin >> x;
  return x;
}

int main() {
  string line;
  ll sum = 0;
  while (getline(cin, line)) {
  }
}
